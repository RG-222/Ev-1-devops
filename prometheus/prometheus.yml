global:
  scrape_interval: 5s # Recolecta métricas cada 5 segundos

scrape_configs:
  - job_name: 'flask_microservice'
    static_configs:
      - targets: ['web_app:5000'] # Apunta al contenedor de la app dentro de la red privada